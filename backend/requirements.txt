# Voice2Sample backend — versiones verificadas en el entorno de desarrollo (WSL, Python 3.12)
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
numpy==1.26.4
soundfile==0.14.0

# Modelo Essentia KNN (los .joblib se entrenaron con scikit-learn 1.9.0;
# usar otra versión puede dar resultados inconsistentes al despickle)
scikit-learn==1.9.0
joblib==1.5.3

# Extracción de descriptores (essentia solo está disponible vía pip en Linux/WSL)
essentia==2.1b6.dev1389

# Modelo CLAP (búsqueda semántica por embeddings)
# transformers >= 5 exige el kwarg `audio=` en ClapProcessor (no `audios=`)
torch==2.12.0
transformers==5.10.2
librosa==0.11.0
